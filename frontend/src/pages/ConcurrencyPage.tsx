import PlaceholderPage from '../components/PlaceholderPage.tsx'

function ConcurrencyPage() {
  return (
    <PlaceholderPage
      eyebrow="CONCURRENCY ISSUES"
      title="Row Locking Under Concurrent Claims"
      description="A static teaching card: real select_for_update code, a sequence diagram, and a real concurrency test log showing serialized writes."
    />
  )
}

export default ConcurrencyPage
